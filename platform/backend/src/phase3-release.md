# Phase 3 release gate

Do not merge until CI has passed for TypeScript, importer tests and Prisma validation/migration. Before production, configure a real database, strong cookie secret, HTTPS, and a real admin account. Verify an official source-backed dataset through upload -> validation -> review -> approval -> publication in a staging environment first.
